name: Publish

on:
  push:
    branches:   
      - main
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  compile:
    name: dart compile exe server
    strategy:
      matrix:
        include:
          - runs-on: ubuntu-latest
            binary-name: server_linux_amd64
            dart-file-name: server.dart

          - runs-on: ubuntu-latest
            binary-name: client_linux_amd64
            dart-file-name: client.dart

          - runs-on: macos-latest
            binary-name: server_macos_amd64
            dart-file-name: server.dart

          - runs-on: macos-latest
            binary-name: client_macos_amd64
            dart-file-name: client.dart

          - runs-on: windows-latest
            binary-name: server_windows.exe
            dart-file-name: server.dart

          - runs-on: windows-latest
            binary-name: client_windows.exe
            dart-file-name: client.dart


    runs-on: ${{ matrix.runs-on }}
    steps:
      - uses: actions/checkout@v2
      - uses: dart-lang/setup-dart@v1
      - run: dart pub get
      - run: mkdir ${{ matrix.runs-on }}
      - run: dart compile exe ${{ matrix.dart-file-name }} -o ${{ matrix.runs-on }}/${{ matrix.binary-name }}
      - uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.binary-name }}
          ####path: ${{ matrix.runs-on }}




  release:
    needs: compile
    name: github release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v2
        with:
          name: bin-ubuntu-latest
          path: bin-linux
      - uses: actions/download-artifact@v2
        with:
          name: bin-macos-latest
          path: bin-macos
      - uses: actions/download-artifact@v2
        with:
          name: bin-windows-latest
          path: bin-windows
      - uses: softprops/action-gh-release@v1
        with:
          draft: true
          files: bin-*/*
